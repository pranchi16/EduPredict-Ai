import { Link,useNavigate } from "react-router-dom"; import { BrainCircuit, ArrowRight } from "lucide-react";
export default function Login(){const nav=useNavigate(); return <Auth title="Welcome back" sub="Sign in to your intelligence workspace."><form onSubmit={e=>{e.preventDefault();nav("/dashboard")}}><label>Email address<input type="email" placeholder="admin@college.edu" required/></label><label>Password<input type="password" placeholder="••••••••" required/></label><div className="form-row"><label className="check"><input type="checkbox"/> Remember me</label><a href="#forgot">Forgot password?</a></div><button className="btn primary full">Sign in <ArrowRight size={17}/></button></form><p className="auth-switch">Don't have an account? <Link to="/register">Create one</Link></p></Auth>}
export function Auth({title,sub,children}){return <div className="auth-page"><div className="auth-visual"><Link className="brand" to="/"><div className="brand-mark"><BrainCircuit/></div><div><b>EduPredict</b><span>AI Intelligence</span></div></Link><div><div className="pill">ACADEMIC INTELLIGENCE</div><h1>Turn student data into better decisions.</h1><p>Predict, analyze, and understand performance through one focused workspace.</p></div></div><div className="auth-box"><div className="auth-card"><span className="eyebrow">SECURE WORKSPACE</span><h2>{title}</h2><p>{sub}</p>{children}</div></div></div>}