import {UploadCloud,FileSpreadsheet,CheckCircle2} from "lucide-react";import {useState} from "react";
export default function Upload(){const [file,setFile]=useState(null);return <><div className="page-intro"><div><span className="eyebrow">DATA / INGESTION</span><h1>Import student data</h1><p>Upload a CSV dataset to send records into the preprocessing pipeline.</p></div></div><div className="upload-layout"><label className="dropzone"><input type="file" accept=".csv" onChange={e=>setFile(e.target.files[0])}/><div className="upload-icon"><UploadCloud/></div><h3>{file?file.name:"Drop your CSV here"}</h3><p>{file?"File selected — ready for validation.":"or click to browse from your computer"}</p><span>CSV · Maximum 25 MB</span></label><div className="panel"><span className="eyebrow">PIPELINE</span><h3>What happens next?</h3>{["Validate column structure","Clean missing values","Encode categorical fields","Engineer model features","Run quality checks"].map((x,i)=><div className="pipeline-step"><b>0{i+1}</b><span>{x}</span>{i===0&&file?<CheckCircle2/>:null}</div>)}</div></div></>}
